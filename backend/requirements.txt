fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1

# Market data sources (install the ones you want to use)
nsepython==2.9        # NSE equities — primary Indian market source
yfinance==0.2.40      # Commodities + global indices — reliable fallback

# Optional: Zerodha Kite Connect (requires API key from https://kite.trade)
# kiteconnect==4.2.0

# Optional: Angel One SmartAPI
# smartapi-python==1.3.5

# Astronomical calculations — Swiss Ephemeris (Lahiri sidereal, used by all Vedic engines)
pyswisseph==2.10.3.2   # Covers 3000 BC–3000 AD; no data files needed (Moshier built-in)

# Model routing — async HTTP for Ollama + Claude API
httpx==0.27.0          # async HTTP client for Ollama streaming
anthropic>=0.40.0      # Claude API (set ANTHROPIC_API_KEY in .env)
